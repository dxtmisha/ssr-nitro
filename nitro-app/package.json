{
  "private": true,
  "scripts": {
    "build": "nitro build",
    "dev": "nitro dev",
    "prepare": "nitro prepare",
    "preview": "node --env-file .env .output/server/index.mjs"
  },
  "devDependencies": {
    "nitropack": "latest"
  }
}
